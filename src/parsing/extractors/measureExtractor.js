// src/parsing/extractors/measureExtractor.js

function extractMeasures(parsedData) {
  // Placeholder para extracción de compases
  return parsedData.measures || [];
}

module.exports = { extractMeasures };